<!-- <p class="alert alert-warning alert-dismissible fade show" style="color: green"><%= notice %></p> --> 
<%# _alerts.html.erb added %>

  <table class="table table-striped table-bordered table-hover text-center">

    <thead class="thead-dark">
      <tr>
        <th>Name</th>
        <%# <th>Last name</th> %>
        <th>Email</th>
        <th>Phone</th>
        <th>Twitter</th>
      </tr>
    </thead>

    <tbody>
        <tr>
          <%= render @friend %>
        </tr>
    </tbody>
  </table>
  
  <div class="d-flex gap-2">
    <%= link_to "Edit", edit_friend_path(@friend), class: "btn btn-secondary" %>
    <%= link_to "Back", friends_path, class: "btn btn-secondary" %>
    <%= button_to "Delete Friend", @friend, method: :delete, class: "btn btn-danger", form: { class: "m-0" } %>
  </div>
